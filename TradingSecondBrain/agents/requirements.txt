# Trimmed to only what the agents code actually imports. crewai pulls in
# litellm (used to talk to Ollama), langchain, opentelemetry, embedchain, etc.
# transitively — we let pip resolve those versions inside the constraints below.

# API server
fastapi>=0.115,<0.120
uvicorn[standard]>=0.30,<0.40
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
httpx>=0.27,<0.29
python-dotenv>=1.0

# Multi-agent orchestration
crewai==0.70.1

# LLM clients — Ollama is the default via crewai/litellm; openai kept for the
# optional cloud fallback gated by ALLOW_CLOUD_LLM.
openai>=1.50,<2.0
ollama>=0.3,<1.0

# Vector store (default). Pin to match the qdrant server image (v1.11.3) —
# client >=1.13 removed the .search() method and warns on version mismatch.
# chromadb is optional — install separately if you set VECTOR_BACKEND=chroma:
#   pip install "chromadb>=0.4.22,<0.5.0"
qdrant-client==1.11.3

# Vault parsing
python-frontmatter>=1.1

# Market data + (hand-rolled) backtester
yfinance>=0.2.44
pandas>=2.2,<3.0
numpy>=1.26,<2.0

# Utilities — tenacity upper bound is forced by crewai's `instructor` dep
tenacity>=8.2.3,<9.0
loguru>=0.7
jsonschema>=4.23
